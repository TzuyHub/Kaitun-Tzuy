-- [[ ULTIMATE ANTI-LAG KAITUN - BLOX FRUITS ]] --

if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local CommF_ = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("CommF_")

getgenv().AntiLagConfig = {
    AutoFarm = true,
    FastAttack = true,
    TweenSpeed = 340,
    AutoStoreFruit = true,
    AutoBuyStyle = true,
    FightingStyle = "Superhuman",
    Team = "Marines",
    FPSBoost = true -- Bật chế độ tối ưu chống giật lag
}

-- 1. Bật chế độ Tối ưu đồ họa / Chống giật (FPS Boost & Potato Graphics)
if getgenv().AntiLagConfig.FPSBoost then
    pcall(function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
        Lighting.GlobalShadows = false
        Lighting.FogEnd = 9e9
        for _, v in pairs(Workspace:GetDescendants()) do
            if v:IsA("Part") or v:IsA("MeshPart") or v:IsA("UnionOperation") then
                v.Material = Enum.Material.SmoothPlastic
                v.Reflectance = 0
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Fire") or v:IsA("Sparkles") then
                v.Enabled = false
            end
        end
    end)
end

-- 2. Giao diện Status giữa màn hình (Thiết kế nhẹ không tốn tài nguyên)
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "AntiLagKaitunGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local statusLabel = Instance.new("TextLabel")
statusLabel.Name = "StatusText"
statusLabel.Size = UDim2.new(0, 450, 0, 45)
statusLabel.Position = UDim2.new(0.5, -225, 0.03, 0)
statusLabel.AnchorPoint = Vector2.new(0.5, 0)
statusLabel.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
statusLabel.BackgroundTransparency = 0.3
statusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
statusLabel.TextSize = 14
statusLabel.Font = Enum.Font.Code
statusLabel.Text = "Status: Đang chạy chế độ chống giật lag..."
statusLabel.Parent = screenGui

local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 6)
uiCorner.Parent = statusLabel

local function updateStatus(text)
    statusLabel.Text = "Status: " .. text
end

-- 3. Hàm Tween mượt mà, nhẹ tải
local function lightTween(targetCFrame)
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    local hrp = char.HumanoidRootPart
    
    local distance = (hrp.Position - targetCFrame.Position).Magnitude
    local timeTravel = distance / getgenv().AntiLagConfig.TweenSpeed
    
    local tweenInfo = TweenInfo.new(timeTravel, Enum.EasingStyle.Linear)
    local tween = TweenService:Create(hrp, tweenInfo, {CFrame = targetCFrame})
    tween:Play()
end

-- 4. Chống AFK
task.spawn(function()
    local vu = game:GetService("VirtualUser")
    LocalPlayer.Idled:Connect(function()
        vu:Button2Down(Vector2.new(0,0), Workspace.CurrentCamera.CFrame)
        task.wait(1)
        vu:Button2Up(Vector2.new(0,0), Workspace.CurrentCamera.CFrame)
    end)
end)

-- 5. Chọn phe tự động
task.spawn(function()
    pcall(function()
        if LocalPlayer.Team == nil then
            CommF_:InvokeServer("SetTeam", getgenv().AntiLagConfig.Team)
        end
    end)
end)

-- 6. Fast Attack tối ưu hóa không gây nặng máy
task.spawn(function()
    while task.wait(0.03) do
        if getgenv().AntiLagConfig.FastAttack then
            pcall(function()
                local combatFramework = debug.getupvalues(require(LocalPlayer.PlayerScripts.CombatFramework))
                local c = combatFramework[2]
                if c and c.activeController then
                    c.activeController.timeToNextAttack = 0
                    c.activeController.hitboxMagnitude = 60
                    c.activeController:attack()
                end
            end)
        end
    end
end)

-- 7. Vòng lặp chính tiết kiệm tài nguyên (Giảm tần suất kiểm tra để mượt game)
task.spawn(function()
    while task.wait(0.2) do -- Tăng nhẹ thời gian chờ vòng lặp để máy không bị quá tải khung hình
        pcall(function()
            local level = LocalPlayer.Data.Level.Value
            
            -- A. Đổi Sea tự động
            if level >= 700 and game.PlaceId == 2753915549 then
                updateStatus("Đạt Level 700 -> Sang Sea 2...")
                CommF_:InvokeServer("GetUnlockMilitaryDetective")
                CommF_:InvokeServer("UnlockMilitaryDetective")
                CommF_:InvokeServer("TravelDressrosa")
                return
            elseif level >= 1500 and game.PlaceId == 4442272183 then
                updateStatus("Đạt Level 1500 -> Sang Sea 3...")
                CommF_:InvokeServer("TravelZou")
                return
            end

            -- B. Nhặt & Cất Trái Cây
            if getgenv().AntiLagConfig.AutoStoreFruit then
                for _, v in pairs(Workspace:GetChildren()) do
                    if v:IsA("Tool") and v.Name:find("Fruit") then
                        updateStatus("Phát hiện trái: " .. v.Name .. " -> Nhặt ngay!")
                        if v:FindFirstChild("Handle") then
                            lightTween(v.Handle.CFrame)
                            task.wait(0.2)
                            firetouchinterest(LocalPlayer.Character.HumanoidRootPart, v.Handle, 0)
                            firetouchinterest(LocalPlayer.Character.HumanoidRootPart, v.Handle, 1)
                            task.wait(0.3)
                        end
                    end
                end

                for _, item in pairs(LocalPlayer.Backpack:GetChildren()) do
                    if item:IsA("Tool") and item.Name:find("Fruit") then
                        updateStatus("Cất trái " .. item.Name .. " vào kho...")
                        CommF_:InvokeServer("StoreFruit", item)
                        task.wait(0.2)
                    end
                end
            end

            -- C. Mua Võ tự động
            if getgenv().AntiLagConfig.AutoBuyStyle then
                CommF_:InvokeServer("BuyItem", getgenv().AntiLagConfig.FightingStyle)
            end

            -- D. Cày Level tự động (Tối ưu hóa tải)
            if getgenv().AntiLagConfig.AutoFarm then
                updateStatus("Level: " .. level .. " | Đang tự động cày cấp & đánh quái...")
                local questGui = LocalPlayer.PlayerGui.Main.Quest
                if not questGui.Visible then
                    CommF_:InvokeServer("}:=?", "StartQuest")
                else
                    updateStatus("Đang hoàn thành nhiệm vụ cấp độ...")
                end
            end
        end)
    end
end)

print("Đã bật chế độ Anti-Lag Kaitun thành công!")
