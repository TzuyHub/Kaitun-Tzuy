-- [[ INSTANT KANITUN SCRIPT - SIÊU TỐC KHÔNG ĐỘ TRỄ ]] --

if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local CommF_ = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("CommF_")

getgenv().InstantConfig = {
    FastAttack = true,
    TweenSpeed = 350, -- Tốc độ cực nhanh
    AutoFruit = true,
    AutoBuyStyle = true,
    FightingStyle = "Superhuman",
    Team = "Marines"
}

-- 1. Giao diện Status giữa màn hình
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "InstantKaitunGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local statusLabel = Instance.new("TextLabel")
statusLabel.Name = "StatusText"
statusLabel.Size = UDim2.new(0, 480, 0, 50)
statusLabel.Position = UDim2.new(0.5, -240, 0.03, 0)
statusLabel.AnchorPoint = Vector2.new(0.5, 0)
statusLabel.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
statusLabel.BackgroundTransparency = 0.2
statusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
statusLabel.TextSize = 15
statusLabel.Font = Enum.Font.GothamBold
statusLabel.Text = "Status: ĐÃ KÍCH HOẠT SIÊU TỐC TỨC THÌ!"
statusLabel.Parent = screenGui

local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 6)
uiCorner.Parent = statusLabel

local function updateStatus(text)
    statusLabel.Text = "Status: " .. text
end

-- 2. Hàm Tween siêu tốc độ lập tức
local function instantTween(targetCFrame)
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    local hrp = char.HumanoidRootPart
    
    local distance = (hrp.Position - targetCFrame.Position).Magnitude
    local timeTravel = distance / getgenv().InstantConfig.TweenSpeed
    
    local tweenInfo = TweenInfo.new(timeTravel, Enum.EasingStyle.Linear)
    local tween = TweenService:Create(hrp, tweenInfo, {CFrame = targetCFrame})
    tween:Play()
end

-- 3. Chống AFK lập tức
task.spawn(function()
    local vu = game:GetService("VirtualUser")
    LocalPlayer.Idled:Connect(function()
        vu:Button2Down(Vector2.new(0,0), Workspace.CurrentCamera.CFrame)
        task.wait(0.5)
        vu:Button2Up(Vector2.new(0,0), Workspace.CurrentCamera.CFrame)
    end)
end)

-- 4. Chọn phe ngay lập tức khi vào game
task.spawn(function()
    pcall(function()
        if LocalPlayer.Team == nil then
            CommF_:InvokeServer("SetTeam", getgenv().InstantConfig.Team)
        end
    end)
end)

-- 5. Fast Attack kích hoạt với độ trễ gần như bằng 0 (0.01 giây)
task.spawn(function()
    while task.wait(0.01) do
        if getgenv().InstantConfig.FastAttack then
            pcall(function()
                local combatFramework = debug.getupvalues(require(LocalPlayer.PlayerScripts.CombatFramework))
                local c = combatFramework[2]
                if c and c.activeController then
                    c.activeController.timeToNextAttack = 0
                    c.activeController.hitboxMagnitude = 70
                    c.activeController:attack()
                end
            end)
        end
    end
end)

-- 6. Vòng lặp phản ứng cực nhanh không giật lag
task.spawn(function()
    while task.wait(0.1) do -- Giảm độ trễ xuống mức tối đa để chạy ngay lập tức
        pcall(function()
            local level = LocalPlayer.Data.Level.Value
            
            -- Chuyển Sea ngay lập tức khi đủ cấp
            if level >= 700 and game.PlaceId == 2753915549 then
                updateStatus("Đạt Level 700 -> Bay sang Sea 2 NGAY LẬP TỨC!")
                CommF_:InvokeServer("GetUnlockMilitaryDetective")
                CommF_:InvokeServer("UnlockMilitaryDetective")
                CommF_:InvokeServer("TravelDressrosa")
                return
            elseif level >= 1500 and game.PlaceId == 4442272183 then
                updateStatus("Đạt Level 1500 -> Bay sang Sea 3 NGAY LẬP TỨC!")
                CommF_:InvokeServer("TravelZou")
                return
            end

            -- Nhặt trái cây siêu tốc ngay khi vừa xuất hiện trong server
            if getgenv().InstantConfig.AutoFruit then
                for _, v in pairs(Workspace:GetChildren()) do
                    if v:IsA("Tool") and v.Name:find("Fruit") then
                        updateStatus("PHÁT HIỆN TRÁI: " .. v.Name .. " -> LAO TỚI NHẶT NGAY!")
                        if v:FindFirstChild("Handle") then
                            instantTween(v.Handle.CFrame)
                            task.wait(0.2)
                            firetouchinterest(LocalPlayer.Character.HumanoidRootPart, v.Handle, 0)
                            firetouchinterest(LocalPlayer.Character.HumanoidRootPart, v.Handle, 1)
                            
                            -- Cất kho lập tức
                            for _, item in pairs(LocalPlayer.Backpack:GetChildren()) do
                                if item:IsA("Tool") and item.Name:find("Fruit") then
                                    CommF_:InvokeServer("StoreFruit", item)
                                end
                            end
                        end
                        return
                    end
                end
            end

            -- Tự động mua võ liên tục
            if getgenv().InstantConfig.AutoBuyStyle then
                CommF_:InvokeServer("BuyItem", getgenv().InstantConfig.FightingStyle)
            end

            updateStatus("Level: " .. level .. " | Sẵn sàng chiến đấu & Săn trái siêu tốc...")
        end)
    end
end)

print("Đã bật chế độ SIÊU TỐC TỨC THÌ!")
