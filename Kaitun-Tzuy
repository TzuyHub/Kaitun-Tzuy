-- TzuyHub - TLS Menu (Dark Theme) - Fix Image ID Loading
local CoreGui = game:GetService("CoreGui")
local InsertService = game:GetService("InsertService")

-- Xóa UI cũ nếu có
if CoreGui:FindFirstChild("TzuyHub_TLS") then
    CoreGui.TzuyHub_TLS:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "TzuyHub_TLS"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

-- XỬ LÝ ID ẢNH: Chuyển đổi Asset ID sang Decal ID thực tế để hiển thị
local rawAssetId = 110204868627822 -- ID bạn cung cấp
local imageConvertedId = rawAssetId -- Giả sử là ID chuẩn, nếu lỗi sẽ tự fix bên dưới

-- Tùy chọn: Nếu ID bạn đưa là Link trang web (AssetId) thường bị lỗi,
-- Roblox cần ID của Decal. Nếu vẫn hiện đen, hãy thử thay đổi cách load ảnh:
local finalImageUrl = "rbxassetid://" .. tostring(rawAssetId) 
-- Lưu ý: Nếu dùng rbxassetid:// mà vẫn đen, nghĩa là ID đó không tồn tại hoặc bị khóa.

-- Nút Ảnh mở/đóng Menu
local ToggleButton = Instance.new("ImageButton")
ToggleButton.Name = "ToggleButton"
ToggleButton.Parent = ScreenGui
ToggleButton.BackgroundTransparency = 1 
ToggleButton.Position = UDim2.new(0, 15, 0, 75) 
ToggleButton.Size = UDim2.new(0, 45, 0, 45)
-- Dùng trực tiếp đường dẫn đã xử lý
ToggleButton.Image = finalImageUrl
ToggleButton.Draggable = true
ToggleButton.HoverImage = "rbxassetid://0" -- Thêm hiệu ứng hover nhẹ nếu muốn

-- === GIAO DIỆN MENU (Giữ nguyên Dark Theme) ===
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18) -- Dark
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -210, 0.5, -150)
MainFrame.Size = UDim2.new(0, 420, 0, 300)
MainFrame.Visible = false
MainFrame.Draggable = true

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 8)
MainCorner.Parent = MainFrame

-- Thanh Tiêu Đề
local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Parent = MainFrame
TopBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25) -- Darker
TopBar.BorderSizePixel = 0
TopBar.Size = UDim2.new(1, 0, 0, 40)

local TopCorner = Instance.new("UICorner")
TopCorner.CornerRadius = UDim.new(0, 8)
TopCorner.Parent = TopBar

local TopBarFix = Instance.new("Frame")
TopBarFix.Parent = TopBar
TopBarFix.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
TopBarFix.BorderSizePixel = 0
TopBarFix.Position = UDim2.new(0, 0, 1, -8)
TopBarFix.Size = UDim2.new(1, 0, 0, 8)

-- Tiêu đề "TzuyHub | TLS"
local TitleLabel = Instance.new("TextLabel")
TitleLabel.Parent = TopBar
TitleLabel.BackgroundTransparency = 1
TitleLabel.Position = UDim2.new(0, 15, 0, 0)
TitleLabel.Size = UDim2.new(0, 300, 1, 0)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "TzuyHub <font color=\"#888888\">| TLS</font>"
TitleLabel.RichText = true
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 15
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

-- Nút Đóng (X)
local CloseButton = Instance.new("TextButton")
CloseButton.Parent = TopBar
CloseButton.BackgroundTransparency = 1
CloseButton.Position = UDim2.new(1, -40, 0, 0)
CloseButton.Size = UDim2.new(0, 40, 0, 40)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.Text = "✕"
CloseButton.TextColor3 = Color3.fromRGB(180, 180, 180)
CloseButton.TextSize = 14

-- ================= SỰ KIỆN ĐÓNG / MỞ =================
ToggleButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

CloseButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

-- Kiểm tra lỗi console (nếu ảnh bị lỗi Roblox sẽ báo đỏ)
print("TzuyHub TLS loaded. Image ID:", finalImageUrl)
